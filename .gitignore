# house compiles files and executable created by cargo
debug/
target/

# add this for libraries, not binaries
# Cargo.lock

# Backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb